RGSS3 -

  classes - 
    Bitmap (gradient_fill_rect, radial_blur)
    Plane <incorporate bitmap tiling>
    Sprite (update) <incorporate wave, flash, color blendings, alpha blending>
    Tilemap (All)
    Viewport (update) <incorporate flash effect>
    Window (All)

  modules - 
    Graphics (transitions, frame_reset, play_movie)
    Input (repeat?)

  ext - 
    Object (msgbox, msgbox_p)
    RGSSAD (rgss3a decryption)
    
    
RGSS2 -

  classes - 
    Bitmap (gradient_fill_rect, radial_blur)
    Plane <incorporate bitmap tiling>
    Sprite (update) <incorporate wave, flash, color blendings, alpha blending>
    Tilemap (All)
    Viewport (update) <incorporate flash effect>
    Window (update[, setup_arrows, setup_border, setup_cursor, setup_background_overlay]) <incorporate all actual drawing>

  modules - 
    Graphics (transitions, frame_reset)
    Input (repeat?)


RGSS1 -

  classes - 
    Plane <incorporate bitmap tiling>
    Sprite (update) <flash, color blendings, alpha blending>
    Tilemap (All)
    Viewport (update) <incorporate flash effect>
    Window (update[, setup_arrows, setup_border, setup_cursor]) <incorporate all actual drawing>

  modules - 
    Graphics (transitions, frame_reset)
    Input (repeat?)