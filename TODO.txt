classes - 
	Bitmap (gradient_fill_rect, blur, radial_blur, text_size, draw_text)
	Plane (All)
	Sprite (All)
	Table (resize)
	Tilemap (All)
	Viewport (update) <incorporate flash effect>
	Window (All)

modules - 
	Audio (All)
	Graphics (update, wait, fadeout, fadein, freeze, transitions, snap_to_bitmap, frame_reset, [latest])
	Input (repeat?)

ext - 
	Object (rgss_main, msgbox, msgbox_p)